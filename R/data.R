# descriptions of mapGCT datasets

#' An example table of column descriptions, as would be
#' parsed from or parse_gct. Initially all the
#' columns are of type character.
"col_desc"

#' An example table of row descriptons, as would be
#' parsed from parse_gct or used to create gct_object
#' by to_GCT
"row_desc"

#' An example of numeric matrix, as would be
#' parsed from parse_gct or used to create gct_object
#' by to_GCT
"gct_mat"

#' An example table of column annotations, which is to be 
#' annotated to existing gct_object.
"col_annotate"